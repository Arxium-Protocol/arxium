[package]
name = "xc-cli"
version = "0.1.0"
edition = "2024"
license.workspace = true

[dependencies]
clap = { workspace = true }
dirs = { workspace = true }
xc-primitives = { workspace = true }

[features]
# Never enabled by default and never part of a production build — see
# RunArgs::inject_fault_at_height's doc comment.
fault-injection = []
